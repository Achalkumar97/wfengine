# Base URL for wfengine REST API (see apps/server). Trailing slash is stripped.
VITE_WFENGINE_API=http://localhost:30001

# Optional: sent as X-API-Key when your server sets API_KEY (see apps/server auth.ts)
# VITE_WFENGINE_API_KEY=
