The Studio app lives at **`apps/studio`**. This folder only keeps a **`tsconfig.json`** shim so editors that still open `examples/studio` resolve TypeScript against the real sources under `apps/studio/src`.

Use:

```bash
npm run dev:studio
```

from the repo root (see root **README.md**).
